# Public Convex deployment URL, injected into the browser for live admin data.
NEXT_PUBLIC_CONVEX_URL=
# Temporary operator gate for the internal tool. Set the same value with:
# npx convex env set OPERATOR_KEY your-long-random-value
NEXT_PUBLIC_OPERATOR_KEY=
