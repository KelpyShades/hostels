# dependencies — every package in the workspace
node_modules/
.pnp
.pnp.*

# next.js — build output and generated files, every package
.next/
out/
next-env.d.ts

# production builds
build/
dist/

# env files — deployment URLs and secrets stay local
.env*
!.env.example

# convex — local artifacts. Note: convex/_generated IS committed
# deliberately, so CI can typecheck without running `convex dev`.
.convex/

# typescript
*.tsbuildinfo

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# vercel
.vercel

# scratch
_temp/
dashboard/node_modules/
landing_page/node_modules/
